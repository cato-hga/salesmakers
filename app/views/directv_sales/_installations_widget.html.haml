%h3 Recent and Upcoming Installations

- recent = @directv_installations.recent_installations
- upcoming = @directv_installations.upcoming_installations

- if @directv_installations.count == 0
  .comment None.
- if recent.count > 0
  #recent_installations
    %h4
      Recent
      %span.small.comment 1 week
    = render partial: 'directv_sales/installations_table', locals: { sales: recent }
- if upcoming.count > 0
  #upcoming_installations
    %h4
      Upcoming
      %span.small.comment 1 week
    = render partial: 'directv_sales/installations_table', locals: { sales: upcoming }