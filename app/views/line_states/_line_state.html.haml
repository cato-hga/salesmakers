.line_state
  = line_state.name
  - unless line_state.locked
    = link_to "&times;".html_safe, remove_state_line_path(@line, line_state), class: [:remove], data: { confirm: 'This action cannot be undone. Are you sure?' }, method: :patch