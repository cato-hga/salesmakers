.device_state
  = device_state.name
  - unless device_state.locked
    = link_to "&times;".html_safe, remove_state_device_path(@device, device_state), class: [:remove], data: { confirm: 'This action cannot be undone. Are you sure?' }, method: :patch